/** Collapsed bottom-sheet height (drag handle + station preview). */
export const MAP_SHEET_PEEK_HEIGHT = 88;

/** Collapsed height with search + locate bar above the filter tabs. */
export const MAP_SHEET_PEEK_WITH_SEARCH = 196;
